import { aspectRatio } from './aspect-ratio'
import { fontSize } from './font-size'
import { hyphenated } from './hyphenated'
import type { Rule } from 'unocss'

export const rules: Rule[] = [aspectRatio, fontSize, hyphenated]
